# Test profile - use in-memory H2 so CI/local tests run without external DB
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Let Hibernate create/drop schema for tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Disable Redis attempts if not available (optional) - comment out if Redis is required in tests
# spring.data.redis.host=localhost
# spring.data.redis.port=6379
